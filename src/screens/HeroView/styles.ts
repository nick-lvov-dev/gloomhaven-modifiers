import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  draw: {
    overflow: 'hidden',
    borderRadius: 32,
    height: 160,
    width: 240,
    marginVertical: 32,
    alignItems: 'center',
    justifyContent: 'center',
  },
  modifier: {
    borderRadius: 32,
    overflow: 'hidden',
    width: 240,
    height: 160,
  },
});
